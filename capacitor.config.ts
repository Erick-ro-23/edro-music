import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edro.music.com',
  appName: 'edro-music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
